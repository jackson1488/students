# Файл: `app/navigation/RootNavigator.js`

## Что это
Главная схема навигации приложения (stack/drawer/web).

## Простыми словами
Этот файл — одна часть фронтенда, которая либо:
- показывает UI,
- хранит состояние,
- общается с backend,
- или настраивает платформу.

## Что делает в проекте
- Импортов в файле: **35**.
- Основная ответственность: Главная схема навигации приложения (stack/drawer/web).

## Главные элементы кода
- `DrawerShell`: отдельный шаг логики внутри этого файла.
- `RootNavigator`: отдельный шаг логики внутри этого файла.
- `buildRoleScreens`: отдельный шаг логики внутри этого файла.
- `screens`: отдельный шаг логики внутри этого файла.

## Как читать новичку
1. Сначала смотри импорты вверху.
2. Затем найди главный экспорт (`export default ...`).
3. После этого пройди функции сверху вниз — они обычно описывают полный сценарий экрана/компонента.
