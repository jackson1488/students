# Файл: `app/services/scheduleService.js`

## Что это
Запросы расписания.

## Простыми словами
Этот файл — одна часть фронтенда, которая либо:
- показывает UI,
- хранит состояние,
- общается с backend,
- или настраивает платформу.

## Что делает в проекте
- Импортов в файле: **1**.
- Основная ответственность: Запросы расписания.

## Главные элементы кода
- `createScheduleEntry`: отдельный шаг логики внутри этого файла.
- `deleteScheduleEntry`: отдельный шаг логики внутри этого файла.
- `fetchScheduleByGroup`: отдельный шаг логики внутри этого файла.
- `updateScheduleEntry`: отдельный шаг логики внутри этого файла.

## Как читать новичку
1. Сначала смотри импорты вверху.
2. Затем найди главный экспорт (`export default ...`).
3. После этого пройди функции сверху вниз — они обычно описывают полный сценарий экрана/компонента.
