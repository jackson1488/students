# Файл: `app/services/chatService.js`

## Что это
Запросы личного/группового чата.

## Простыми словами
Этот файл — одна часть фронтенда, которая либо:
- показывает UI,
- хранит состояние,
- общается с backend,
- или настраивает платформу.

## Что делает в проекте
- Импортов в файле: **1**.
- Основная ответственность: Запросы личного/группового чата.

## Главные элементы кода
- `appendAttachment`: отдельный шаг логики внутри этого файла.
- `buildAttachmentPart`: отдельный шаг логики внутри этого файла.
- `clearChatForMe`: отдельный шаг логики внутри этого файла.
- `deleteMessageForMe`: отдельный шаг логики внутри этого файла.
- `fetchChatContacts`: отдельный шаг логики внутри этого файла.
- `fetchGroupChatRooms`: отдельный шаг логики внутри этого файла.
- `fetchGroupMessages`: отдельный шаг логики внутри этого файла.
- `fetchMessages`: отдельный шаг логики внутри этого файла.
- `sendGroupMessage`: отдельный шаг логики внутри этого файла.
- `sendMessage`: отдельный шаг логики внутри этого файла.

## Как читать новичку
1. Сначала смотри импорты вверху.
2. Затем найди главный экспорт (`export default ...`).
3. После этого пройди функции сверху вниз — они обычно описывают полный сценарий экрана/компонента.
