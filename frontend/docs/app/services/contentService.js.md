# Файл: `app/services/contentService.js`

## Что это
Запросы новостей и библиотеки/контента.

## Простыми словами
Этот файл — одна часть фронтенда, которая либо:
- показывает UI,
- хранит состояние,
- общается с backend,
- или настраивает платформу.

## Что делает в проекте
- Импортов в файле: **2**.
- Основная ответственность: Запросы новостей и библиотеки/контента.

## Главные элементы кода
- `buildImagePart`: отдельный шаг логики внутри этого файла.
- `createBook`: отдельный шаг логики внутри этого файла.
- `createNews`: отдельный шаг логики внутри этого файла.
- `deleteBookShelfItem`: отдельный шаг логики внутри этого файла.
- `fetchBookCatalog`: отдельный шаг логики внутри этого файла.
- `fetchBookShelf`: отдельный шаг логики внутри этого файла.
- `fetchBooks`: отдельный шаг логики внутри этого файла.
- `fetchNews`: отдельный шаг логики внутри этого файла.
- `isWebFile`: отдельный шаг логики внутри этого файла.
- `updateNews`: отдельный шаг логики внутри этого файла.
- `updateNewsStatus`: отдельный шаг логики внутри этого файла.
- `upsertBookShelfItem`: отдельный шаг логики внутри этого файла.

## Как читать новичку
1. Сначала смотри импорты вверху.
2. Затем найди главный экспорт (`export default ...`).
3. После этого пройди функции сверху вниз — они обычно описывают полный сценарий экрана/компонента.
