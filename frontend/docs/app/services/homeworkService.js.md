# Файл: `app/services/homeworkService.js`

## Что это
Запросы домашних заданий и сдач.

## Простыми словами
Этот файл — одна часть фронтенда, которая либо:
- показывает UI,
- хранит состояние,
- общается с backend,
- или настраивает платформу.

## Что делает в проекте
- Импортов в файле: **1**.
- Основная ответственность: Запросы домашних заданий и сдач.

## Главные элементы кода
- `appendAttachment`: отдельный шаг логики внутри этого файла.
- `buildAttachmentPart`: отдельный шаг логики внутри этого файла.
- `createHomework`: отдельный шаг логики внутри этого файла.
- `deleteHomework`: отдельный шаг логики внутри этого файла.
- `fetchHomeworkByGroup`: отдельный шаг логики внутри этого файла.
- `fetchHomeworkSubmissions`: отдельный шаг логики внутри этого файла.
- `fetchHomeworkTargets`: отдельный шаг логики внутри этого файла.
- `reviewHomeworkSubmission`: отдельный шаг логики внутри этого файла.
- `submitHomework`: отдельный шаг логики внутри этого файла.
- `updateHomeworkStatus`: отдельный шаг логики внутри этого файла.

## Как читать новичку
1. Сначала смотри импорты вверху.
2. Затем найди главный экспорт (`export default ...`).
3. После этого пройди функции сверху вниз — они обычно описывают полный сценарий экрана/компонента.
