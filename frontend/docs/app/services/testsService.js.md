# Файл: `app/services/testsService.js`

## Что это
Запросы тестирования (создание/прохождение/результаты).

## Простыми словами
Этот файл — одна часть фронтенда, которая либо:
- показывает UI,
- хранит состояние,
- общается с backend,
- или настраивает платформу.

## Что делает в проекте
- Импортов в файле: **1**.
- Основная ответственность: Запросы тестирования (создание/прохождение/результаты).

## Главные элементы кода
- `activateTest`: отдельный шаг логики внутри этого файла.
- `allowRetake`: отдельный шаг логики внутри этого файла.
- `createTest`: отдельный шаг логики внутри этого файла.
- `deactivateTest`: отдельный шаг логики внутри этого файла.
- `fetchModuleSummary`: отдельный шаг логики внутри этого файла.
- `fetchTestDetail`: отдельный шаг логики внутри этого файла.
- `fetchTests`: отдельный шаг логики внутри этого файла.
- `saveModuleSummary`: отдельный шаг логики внутри этого файла.
- `startTestAttempt`: отдельный шаг логики внутри этого файла.
- `submitTest`: отдельный шаг логики внутри этого файла.
- `syncTestProgress`: отдельный шаг логики внутри этого файла.
- `updateTest`: отдельный шаг логики внутри этого файла.

## Как читать новичку
1. Сначала смотри импорты вверху.
2. Затем найди главный экспорт (`export default ...`).
3. После этого пройди функции сверху вниз — они обычно описывают полный сценарий экрана/компонента.
