# Файл: `app/services/usersService.js`

## Что это
Запросы к API пользователей и профиля.

## Простыми словами
Этот файл — одна часть фронтенда, которая либо:
- показывает UI,
- хранит состояние,
- общается с backend,
- или настраивает платформу.

## Что делает в проекте
- Импортов в файле: **1**.
- Основная ответственность: Запросы к API пользователей и профиля.

## Главные элементы кода
- `buildImagePart`: отдельный шаг логики внутри этого файла.
- `createUser`: отдельный шаг логики внутри этого файла.
- `fetchMyDetails`: отдельный шаг логики внутри этого файла.
- `fetchMyProfile`: отдельный шаг логики внутри этого файла.
- `fetchUsers`: отдельный шаг логики внутри этого файла.
- `removeUser`: отдельный шаг логики внутри этого файла.
- `updateMyDetails`: отдельный шаг логики внутри этого файла.
- `uploadMyAvatar`: отдельный шаг логики внутри этого файла.

## Как читать новичку
1. Сначала смотри импорты вверху.
2. Затем найди главный экспорт (`export default ...`).
3. После этого пройди функции сверху вниз — они обычно описывают полный сценарий экрана/компонента.
