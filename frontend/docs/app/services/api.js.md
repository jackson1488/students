# Файл: `app/services/api.js`

## Что это
Базовый HTTP-клиент (axios), baseURL и JWT-интерсепторы.

## Простыми словами
Этот файл — одна часть фронтенда, которая либо:
- показывает UI,
- хранит состояние,
- общается с backend,
- или настраивает платформу.

## Что делает в проекте
- Импортов в файле: **4**.
- Основная ответственность: Базовый HTTP-клиент (axios), baseURL и JWT-интерсепторы.

## Главные элементы кода
- `extractHost`: отдельный шаг логики внутри этого файла.
- `inferDeviceHost`: отдельный шаг логики внутри этого файла.
- `localizeApiErrorMessage`: отдельный шаг логики внутри этого файла.
- `resolveBaseURL`: отдельный шаг логики внутри этого файла.
- `setAuthToken`: отдельный шаг логики внутри этого файла.

## Как читать новичку
1. Сначала смотри импорты вверху.
2. Затем найди главный экспорт (`export default ...`).
3. После этого пройди функции сверху вниз — они обычно описывают полный сценарий экрана/компонента.
