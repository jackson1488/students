# Файл: `app/services/gradesService.js`

## Что это
Запросы оценок и журнальной логики.

## Простыми словами
Этот файл — одна часть фронтенда, которая либо:
- показывает UI,
- хранит состояние,
- общается с backend,
- или настраивает платформу.

## Что делает в проекте
- Импортов в файле: **1**.
- Основная ответственность: Запросы оценок и журнальной логики.

## Главные элементы кода
- `createGrade`: отдельный шаг логики внутри этого файла.
- `fetchGradeJournal`: отдельный шаг логики внутри этого файла.
- `fetchGradesByStudent`: отдельный шаг логики внутри этого файла.
- `saveGradeJournalValue`: отдельный шаг логики внутри этого файла.

## Как читать новичку
1. Сначала смотри импорты вверху.
2. Затем найди главный экспорт (`export default ...`).
3. После этого пройди функции сверху вниз — они обычно описывают полный сценарий экрана/компонента.
