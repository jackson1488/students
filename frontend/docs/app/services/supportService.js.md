# Файл: `app/services/supportService.js`

## Что это
Запросы раздела поддержки.

## Простыми словами
Этот файл — одна часть фронтенда, которая либо:
- показывает UI,
- хранит состояние,
- общается с backend,
- или настраивает платформу.

## Что делает в проекте
- Импортов в файле: **1**.
- Основная ответственность: Запросы раздела поддержки.

## Главные элементы кода
- `appendAttachment`: отдельный шаг логики внутри этого файла.
- `buildImagePart`: отдельный шаг логики внутри этого файла.
- `fetchSupportContacts`: отдельный шаг логики внутри этого файла.
- `fetchSupportMessages`: отдельный шаг логики внутри этого файла.
- `sendSupportMessage`: отдельный шаг логики внутри этого файла.

## Как читать новичку
1. Сначала смотри импорты вверху.
2. Затем найди главный экспорт (`export default ...`).
3. После этого пройди функции сверху вниз — они обычно описывают полный сценарий экрана/компонента.
