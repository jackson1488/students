# Файл: `app/services/academyService.js`

## Что это
Запросы к структуре обучения (группы/преподаватели/студенты/привязки).

## Простыми словами
Этот файл — одна часть фронтенда, которая либо:
- показывает UI,
- хранит состояние,
- общается с backend,
- или настраивает платформу.

## Что делает в проекте
- Импортов в файле: **1**.
- Основная ответственность: Запросы к структуре обучения (группы/преподаватели/студенты/привязки).

## Главные элементы кода
- `createBinding`: отдельный шаг логики внутри этого файла.
- `createGroup`: отдельный шаг логики внутри этого файла.
- `createStudent`: отдельный шаг логики внутри этого файла.
- `createTeacher`: отдельный шаг логики внутри этого файла.
- `fetchBindings`: отдельный шаг логики внутри этого файла.
- `fetchGroups`: отдельный шаг логики внутри этого файла.
- `fetchStudentById`: отдельный шаг логики внутри этого файла.
- `fetchStudents`: отдельный шаг логики внутри этого файла.
- `fetchTeacherById`: отдельный шаг логики внутри этого файла.
- `fetchTeachers`: отдельный шаг логики внутри этого файла.
- `updateGroup`: отдельный шаг логики внутри этого файла.
- `updateStudent`: отдельный шаг логики внутри этого файла.
- `updateTeacher`: отдельный шаг логики внутри этого файла.

## Как читать новичку
1. Сначала смотри импорты вверху.
2. Затем найди главный экспорт (`export default ...`).
3. После этого пройди функции сверху вниз — они обычно описывают полный сценарий экрана/компонента.
