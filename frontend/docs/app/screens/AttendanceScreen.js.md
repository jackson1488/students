# Файл: `app/screens/AttendanceScreen.js`

## Что это
Экран посещаемости.

## Простыми словами
Этот файл — одна часть фронтенда, которая либо:
- показывает UI,
- хранит состояние,
- общается с backend,
- или настраивает платформу.

## Что делает в проекте
- Импортов в файле: **12**.
- Основная ответственность: Экран посещаемости.

## Главные элементы кода
- `AttendanceScreen`: отдельный шаг логики внутри этого файла.
- `loadAttendance`: отдельный шаг логики внутри этого файла.
- `onCreateAttendance`: отдельный шаг логики внутри этого файла.

## Как читать новичку
1. Сначала смотри импорты вверху.
2. Затем найди главный экспорт (`export default ...`).
3. После этого пройди функции сверху вниз — они обычно описывают полный сценарий экрана/компонента.
