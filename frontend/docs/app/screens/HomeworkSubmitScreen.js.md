# Файл: `app/screens/HomeworkSubmitScreen.js`

## Что это
Экран отправки решения домашнего задания.

## Простыми словами
Этот файл — одна часть фронтенда, которая либо:
- показывает UI,
- хранит состояние,
- общается с backend,
- или настраивает платформу.

## Что делает в проекте
- Импортов в файле: **10**.
- Основная ответственность: Экран отправки решения домашнего задания.

## Главные элементы кода
- `HomeworkSubmitScreen`: отдельный шаг логики внутри этого файла.
- `canSubmit`: отдельный шаг логики внутри этого файла.
- `firstSubmission`: отдельный шаг логики внутри этого файла.
- `loadMySubmission`: отдельный шаг логики внутри этого файла.
- `normalizedStatus`: отдельный шаг логики внутри этого файла.
- `onSubmit`: отдельный шаг логики внутри этого файла.
- `pickAttachment`: отдельный шаг логики внутри этого файла.
- `statusLabel`: отдельный шаг логики внутри этого файла.

## Как читать новичку
1. Сначала смотри импорты вверху.
2. Затем найди главный экспорт (`export default ...`).
3. После этого пройди функции сверху вниз — они обычно описывают полный сценарий экрана/компонента.
