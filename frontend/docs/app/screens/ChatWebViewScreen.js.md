# Файл: `app/screens/ChatWebViewScreen.js`

## Что это
Встроенный webview для открытия ссылок из чата.

## Простыми словами
Этот файл — одна часть фронтенда, которая либо:
- показывает UI,
- хранит состояние,
- общается с backend,
- или настраивает платформу.

## Что делает в проекте
- Импортов в файле: **6**.
- Основная ответственность: Встроенный webview для открытия ссылок из чата.

## Главные элементы кода
- `ChatWebViewScreen`: отдельный шаг логики внутри этого файла.
- `normalizeUrl`: отдельный шаг логики внутри этого файла.
- `url`: отдельный шаг логики внутри этого файла.

## Как читать новичку
1. Сначала смотри импорты вверху.
2. Затем найди главный экспорт (`export default ...`).
3. После этого пройди функции сверху вниз — они обычно описывают полный сценарий экрана/компонента.
