# Файл: `app/screens/ChatThreadScreen.js`

## Что это
Экран конкретного диалога/комнаты сообщений.

## Простыми словами
Этот файл — одна часть фронтенда, которая либо:
- показывает UI,
- хранит состояние,
- общается с backend,
- или настраивает платформу.

## Что делает в проекте
- Импортов в файле: **14**.
- Основная ответственность: Экран конкретного диалога/комнаты сообщений.

## Главные элементы кода
- `ChatThreadScreen`: отдельный шаг логики внутри этого файла.
- `buildThreadItems`: отдельный шаг логики внутри этого файла.
- `canSend`: отдельный шаг логики внутри этого файла.
- `chatTitle`: отдельный шаг логики внутри этого файла.
- `confirmDeleteForMe`: отдельный шаг логики внутри этого файла.
- `dayKey`: отдельный шаг логики внутри этого файла.
- `formatDayLabel`: отдельный шаг логики внутри этого файла.
- `formatTime`: отдельный шаг логики внутри этого файла.
- `getAttachmentName`: отдельный шаг логики внутри этого файла.
- `hasRowChanges`: отдельный шаг логики внутри этого файла.
- `initialsFromName`: отдельный шаг логики внутри этого файла.
- `isImageAttachment`: отдельный шаг логики внутри этого файла.
- `leftAction`: отдельный шаг логики внутри этого файла.
- `loadConversation`: отдельный шаг логики внутри этого файла.
- `loadPartnerMeta`: отдельный шаг логики внутри этого файла.
- `matched`: отдельный шаг логики внутри этого файла.
- `mergeMessages`: отдельный шаг логики внутри этого файла.
- `normalizeMessages`: отдельный шаг логики внутри этого файла.
- `onClearChat`: отдельный шаг логики внутри этого файла.
- `onDeleteForMe`: отдельный шаг логики внутри этого файла.

## Как читать новичку
1. Сначала смотри импорты вверху.
2. Затем найди главный экспорт (`export default ...`).
3. После этого пройди функции сверху вниз — они обычно описывают полный сценарий экрана/компонента.
