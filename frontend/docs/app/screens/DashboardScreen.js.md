# Файл: `app/screens/DashboardScreen.js`

## Что это
Главный экран роли с обзорной информацией.

## Простыми словами
Этот файл — одна часть фронтенда, которая либо:
- показывает UI,
- хранит состояние,
- общается с backend,
- или настраивает платформу.

## Что делает в проекте
- Импортов в файле: **19**.
- Основная ответственность: Главный экран роли с обзорной информацией.

## Главные элементы кода
- `DashboardBarChart`: отдельный шаг логики внутри этого файла.
- `DashboardScreen`: отдельный шаг логики внутри этого файла.
- `ProgressRow`: отдельный шаг логики внутри этого файла.
- `attemptedCount`: отдельный шаг логики внутри этого файла.
- `attempts`: отдельный шаг логики внутри этого файла.
- `avatarUri`: отдельный шаг логики внутри этого файла.
- `buildInsights`: отдельный шаг логики внутри этого файла.
- `calculateAttemptedRate`: отдельный шаг логики внутри этого файла.
- `calculateAttendanceRate`: отдельный шаг логики внутри этого файла.
- `calculateAverageGrade`: отдельный шаг логики внутри этого файла.
- `calculateTestScoreRate`: отдельный шаг логики внутри этого файла.
- `countForChart`: отдельный шаг логики внутри этого файла.
- `countMax`: отдельный шаг логики внутри этого файла.
- `countStats`: отдельный шаг логики внутри этого файла.
- `formatGrade`: отдельный шаг логики внутри этого файла.
- `formatPercent`: отдельный шаг логики внутри этого файла.
- `getTodayLessonsCount`: отдельный шаг логики внутри этого файла.
- `goToMyData`: отдельный шаг логики внутри этого файла.
- `goToSettings`: отдельный шаг логики внутри этого файла.
- `initials`: отдельный шаг логики внутри этого файла.

## Как читать новичку
1. Сначала смотри импорты вверху.
2. Затем найди главный экспорт (`export default ...`).
3. После этого пройди функции сверху вниз — они обычно описывают полный сценарий экрана/компонента.
