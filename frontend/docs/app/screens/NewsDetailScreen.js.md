# Файл: `app/screens/NewsDetailScreen.js`

## Что это
Подробный просмотр одной новости.

## Простыми словами
Этот файл — одна часть фронтенда, которая либо:
- показывает UI,
- хранит состояние,
- общается с backend,
- или настраивает платформу.

## Что делает в проекте
- Импортов в файле: **7**.
- Основная ответственность: Подробный просмотр одной новости.

## Главные элементы кода
- `NewsDetailScreen`: отдельный шаг логики внутри этого файла.
- `match`: отдельный шаг логики внутри этого файла.
- `normalizeDayValue`: отдельный шаг логики внутри этого файла.
- `readableDate`: отдельный шаг логики внутри этого файла.
- `resolveImageUrl`: отдельный шаг логики внутри этого файла.

## Как читать новичку
1. Сначала смотри импорты вверху.
2. Затем найди главный экспорт (`export default ...`).
3. После этого пройди функции сверху вниз — они обычно описывают полный сценарий экрана/компонента.
