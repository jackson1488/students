# Файл: `app/screens/HomeworkScreen.js`

## Что это
Список домашних заданий.

## Простыми словами
Этот файл — одна часть фронтенда, которая либо:
- показывает UI,
- хранит состояние,
- общается с backend,
- или настраивает платформу.

## Что делает в проекте
- Импортов в файле: **12**.
- Основная ответственность: Список домашних заданий.

## Главные элементы кода
- `HomeworkScreen`: отдельный шаг логики внутри этого файла.
- `groupsForSubject`: отдельный шаг логики внутри этого файла.
- `ids`: отдельный шаг логики внутри этого файла.
- `loadAdminGroups`: отдельный шаг логики внутри этого файла.
- `loadTeacherTargets`: отдельный шаг логики внутри этого файла.
- `names`: отдельный шаг логики внутри этого файла.
- `normalizeArray`: отдельный шаг логики внутри этого файла.
- `normalizeGroupRows`: отдельный шаг логики внутри этого файла.
- `normalizeStudentRows`: отдельный шаг логики внутри этого файла.
- `onCreate`: отдельный шаг логики внутри этого файла.
- `onRefresh`: отдельный шаг логики внутри этого файла.
- `openSubject`: отдельный шаг логики внутри этого файла.
- `readableDate`: отдельный шаг логики внутри этого файла.
- `studentsForCreateGroup`: отдельный шаг логики внутри этого файла.
- `subjectBuckets`: отдельный шаг логики внутри этого файла.
- `viewGroups`: отдельный шаг логики внутри этого файла.

## Как читать новичку
1. Сначала смотри импорты вверху.
2. Затем найди главный экспорт (`export default ...`).
3. После этого пройди функции сверху вниз — они обычно описывают полный сценарий экрана/компонента.
