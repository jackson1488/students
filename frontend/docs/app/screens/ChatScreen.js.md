# Файл: `app/screens/ChatScreen.js`

## Что это
Список чатов и вход в переписку.

## Простыми словами
Этот файл — одна часть фронтенда, которая либо:
- показывает UI,
- хранит состояние,
- общается с backend,
- или настраивает платформу.

## Что делает в проекте
- Импортов в файле: **12**.
- Основная ответственность: Список чатов и вход в переписку.

## Главные элементы кода
- `ChatScreen`: отдельный шаг логики внутри этого файла.
- `adminGroupOptions`: отдельный шаг логики внутри этого файла.
- `adminVisibleContacts`: отдельный шаг логики внутри этого файла.
- `groups`: отдельный шаг логики внутри этого файла.
- `initialsFromName`: отдельный шаг логики внутри этого файла.
- `listRows`: отдельный шаг логики внутри этого файла.
- `normalizeGroupKey`: отдельный шаг логики внутри этого файла.
- `onRefresh`: отдельный шаг логики внутри этого файла.
- `openDirectChat`: отдельный шаг логики внутри этого файла.
- `openGroupChat`: отдельный шаг логики внутри этого файла.
- `openGroupFromStudent`: отдельный шаг логики внутри этого файла.
- `openGroupFromTeacher`: отдельный шаг логики внутри этого файла.
- `renderAdminFilters`: отдельный шаг логики внутри этого файла.
- `renderAvatar`: отдельный шаг логики внутри этого файла.
- `renderContactRow`: отдельный шаг логики внутри этого файла.
- `renderRoomRow`: отдельный шаг логики внутри этого файла.
- `renderTeacherFilters`: отдельный шаг логики внутри этого файла.
- `resolveMediaUrl`: отдельный шаг логики внутри этого файла.
- `roomKey`: отдельный шаг логики внутри этого файла.
- `studentVisibleContacts`: отдельный шаг логики внутри этого файла.

## Как читать новичку
1. Сначала смотри импорты вверху.
2. Затем найди главный экспорт (`export default ...`).
3. После этого пройди функции сверху вниз — они обычно описывают полный сценарий экрана/компонента.
