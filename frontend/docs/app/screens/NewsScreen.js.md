# Файл: `app/screens/NewsScreen.js`

## Что это
Список новостей и объявлений.

## Простыми словами
Этот файл — одна часть фронтенда, которая либо:
- показывает UI,
- хранит состояние,
- общается с backend,
- или настраивает платформу.

## Что делает в проекте
- Импортов в файле: **16**.
- Основная ответственность: Список новостей и объявлений.

## Главные элементы кода
- `NewsScreen`: отдельный шаг логики внутри этого файла.
- `beginEditNews`: отдельный шаг логики внутри этого файла.
- `buildReplacementPayload`: отдельный шаг логики внутри этого файла.
- `closeViewer`: отдельный шаг логики внутри этого файла.
- `formatNewsDate`: отдельный шаг логики внутри этого файла.
- `getDayLabel`: отдельный шаг логики внутри этого файла.
- `hasGroup`: отдельный шаг логики внутри этого файла.
- `loadGroups`: отдельный шаг логики внутри этого файла.
- `loadNews`: отдельный шаг логики внутри этого файла.
- `loadReplacementBindings`: отдельный шаг логики внутри этого файла.
- `loadScheduleForReplacementGroup`: отдельный шаг логики внутри этого файла.
- `markImageBroken`: отдельный шаг логики внутри этого файла.
- `match`: отдельный шаг логики внутри этого файла.
- `normalizeDayValue`: отдельный шаг логики внутри этого файла.
- `onCreateNews`: отдельный шаг логики внутри этого файла.
- `onToggleNewsStatus`: отдельный шаг логики внутри этого файла.
- `openNews`: отдельный шаг логики внутри этого файла.
- `pickImageFile`: отдельный шаг логики внутри этого файла.
- `replacementSlots`: отдельный шаг логики внутри этого файла.
- `replacementTargets`: отдельный шаг логики внутри этого файла.

## Как читать новичку
1. Сначала смотри импорты вверху.
2. Затем найди главный экспорт (`export default ...`).
3. После этого пройди функции сверху вниз — они обычно описывают полный сценарий экрана/компонента.
