# Файл: `app/screens/HomeworkDetailScreen.js`

## Что это
Подробный просмотр домашнего задания.

## Простыми словами
Этот файл — одна часть фронтенда, которая либо:
- показывает UI,
- хранит состояние,
- общается с backend,
- или настраивает платформу.

## Что делает в проекте
- Импортов в файле: **15**.
- Основная ответственность: Подробный просмотр домашнего задания.

## Главные элементы кода
- `HomeworkDetailScreen`: отдельный шаг логики внутри этого файла.
- `completedQueueRows`: отдельный шаг логики внутри этого файла.
- `filtered`: отдельный шаг логики внутри этого файла.
- `isArchivedStatus`: отдельный шаг логики внутри этого файла.
- `isCompletedStatus`: отдельный шаг логики внутри этого файла.
- `isNeedsFixStatus`: отдельный шаг логики внутри этого файла.
- `loadSubjectHomework`: отдельный шаг логики внутри этого файла.
- `normalizeRows`: отдельный шаг логики внутри этого файла.
- `normalizedStatus`: отдельный шаг логики внутри этого файла.
- `onDeleteHomework`: отдельный шаг логики внутри этого файла.
- `onExport`: отдельный шаг логики внутри этого файла.
- `onQuickSubmissionAction`: отдельный шаг логики внутри этого файла.
- `onSaveHomeworkEdit`: отдельный шаг логики внутри этого файла.
- `onSaveSubmissionReview`: отдельный шаг логики внутри этого файла.
- `onShareImage`: отдельный шаг логики внутри этого файла.
- `onShareText`: отдельный шаг логики внутри этого файла.
- `onToggleHomeworkActive`: отдельный шаг логики внутри этого файла.
- `onToggleHomeworkArchive`: отдельный шаг логики внутри этого файла.
- `openAttachment`: отдельный шаг логики внутри этого файла.
- `openSubmitScreen`: отдельный шаг логики внутри этого файла.

## Как читать новичку
1. Сначала смотри импорты вверху.
2. Затем найди главный экспорт (`export default ...`).
3. После этого пройди функции сверху вниз — они обычно описывают полный сценарий экрана/компонента.
