# Файл: `app/screens/LibraryReaderScreen.js`

## Что это
Экран чтения книги во встроенном просмотрщике.

## Простыми словами
Этот файл — одна часть фронтенда, которая либо:
- показывает UI,
- хранит состояние,
- общается с backend,
- или настраивает платформу.

## Что делает в проекте
- Импортов в файле: **9**.
- Основная ответственность: Экран чтения книги во встроенном просмотрщике.

## Главные элементы кода
- `LibraryReaderScreen`: отдельный шаг логики внутри этого файла.
- `buildReaderUrl`: отдельный шаг логики внутри этого файла.
- `normalizeUrl`: отдельный шаг логики внутри этого файла.
- `onToggleBookmark`: отдельный шаг логики внутри этого файла.
- `onToggleFavorite`: отдельный шаг логики внутри этого файла.
- `onToggleRead`: отдельный шаг логики внутри этого файла.
- `sourceUrl`: отдельный шаг логики внутри этого файла.
- `webViewSource`: отдельный шаг логики внутри этого файла.

## Как читать новичку
1. Сначала смотри импорты вверху.
2. Затем найди главный экспорт (`export default ...`).
3. После этого пройди функции сверху вниз — они обычно описывают полный сценарий экрана/компонента.
