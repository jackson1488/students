# Файл: `app/screens/GradesScreen.js`

## Что это
Экран оценок пользователя/журнала преподавателя.

## Простыми словами
Этот файл — одна часть фронтенда, которая либо:
- показывает UI,
- хранит состояние,
- общается с backend,
- или настраивает платформу.

## Что делает в проекте
- Импортов в файле: **16**.
- Основная ответственность: Экран оценок пользователя/журнала преподавателя.

## Главные элементы кода
- `GradesScreen`: отдельный шаг логики внутри этого файла.
- `buildDatesByWeekdays`: отдельный шаг логики внутри этого файла.
- `closeJournalCell`: отдельный шаг логики внутри этого файла.
- `dateColumns`: отдельный шаг логики внутри этого файла.
- `formSource`: отдельный шаг логики внутри этого файла.
- `gradeColumns`: отдельный шаг логики внутри этого файла.
- `journalDates`: отдельный шаг логики внутри этого файла.
- `journalRows`: отдельный шаг логики внутри этого файла.
- `loadStudentScheduleMeta`: отдельный шаг логики внутри этого файла.
- `moduleRows`: отдельный шаг логики внутри этого файла.
- `normalizeJournalGradeValue`: отдельный шаг логики внутри этого файла.
- `onExport`: отдельный шаг логики внутри этого файла.
- `onRefresh`: отдельный шаг логики внутри этого файла.
- `onSaveModule`: отдельный шаг логики внутри этого файла.
- `ownEntries`: отдельный шаг логики внутри этого файла.
- `ownSubjects`: отдельный шаг логики внутри этого файла.
- `readableDate`: отдельный шаг логики внутри этого файла.
- `resultRows`: отдельный шаг логики внутри этого файла.
- `shortDateLabel`: отдельный шаг логики внутри этого файла.
- `studentGradeColumns`: отдельный шаг логики внутри этого файла.

## Как читать новичку
1. Сначала смотри импорты вверху.
2. Затем найди главный экспорт (`export default ...`).
3. После этого пройди функции сверху вниз — они обычно описывают полный сценарий экрана/компонента.
