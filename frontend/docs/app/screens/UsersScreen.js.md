# Файл: `app/screens/UsersScreen.js`

## Что это
Админский экран управления пользователями и структурой.

## Простыми словами
Этот файл — одна часть фронтенда, которая либо:
- показывает UI,
- хранит состояние,
- общается с backend,
- или настраивает платформу.

## Что делает в проекте
- Импортов в файле: **13**.
- Основная ответственность: Админский экран управления пользователями и структурой.

## Главные элементы кода
- `UsersScreen`: отдельный шаг логики внутри этого файла.
- `calculateCourse`: отдельный шаг логики внутри этого файла.
- `cancelEditStudent`: отдельный шаг логики внутри этого файла.
- `cancelEditTeacher`: отдельный шаг логики внутри этого файла.
- `exists`: отдельный шаг логики внутри этого файла.
- `extractStudentCode`: отдельный шаг логики внутри этого файла.
- `loadAll`: отдельный шаг логики внутри этого файла.
- `loadBindings`: отдельный шаг логики внутри этого файла.
- `loadGroups`: отдельный шаг логики внутри этого файла.
- `loadStudents`: отдельный шаг логики внутри этого файла.
- `loadTeachers`: отдельный шаг логики внутри этого файла.
- `normalizeId`: отдельный шаг логики внутри этого файла.
- `onCreateBinding`: отдельный шаг логики внутри этого файла.
- `onCreateGroup`: отдельный шаг логики внутри этого файла.
- `onCreateStudent`: отдельный шаг логики внутри этого файла.
- `onCreateTeacher`: отдельный шаг логики внутри этого файла.
- `onDeleteUser`: отдельный шаг логики внутри этого файла.
- `onUpdateGroupCourse`: отдельный шаг логики внутри этого файла.
- `renderBindingsTab`: отдельный шаг логики внутри этого файла.
- `renderGroupsTab`: отдельный шаг логики внутри этого файла.

## Как читать новичку
1. Сначала смотри импорты вверху.
2. Затем найди главный экспорт (`export default ...`).
3. После этого пройди функции сверху вниз — они обычно описывают полный сценарий экрана/компонента.
