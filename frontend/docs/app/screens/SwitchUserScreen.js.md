# Файл: `app/screens/SwitchUserScreen.js`

## Что это
Быстрое переключение пользователя (админ).

## Простыми словами
Этот файл — одна часть фронтенда, которая либо:
- показывает UI,
- хранит состояние,
- общается с backend,
- или настраивает платформу.

## Что делает в проекте
- Импортов в файле: **10**.
- Основная ответственность: Быстрое переключение пользователя (админ).

## Главные элементы кода
- `SwitchUserScreen`: отдельный шаг логики внутри этого файла.
- `filtered`: отдельный шаг логики внутри этого файла.
- `handleSwitch`: отдельный шаг логики внутри этого файла.
- `loadUsers`: отдельный шаг логики внутри этого файла.
- `visibleUsers`: отдельный шаг логики внутри этого файла.

## Как читать новичку
1. Сначала смотри импорты вверху.
2. Затем найди главный экспорт (`export default ...`).
3. После этого пройди функции сверху вниз — они обычно описывают полный сценарий экрана/компонента.
