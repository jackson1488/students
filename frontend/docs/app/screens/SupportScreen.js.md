# Файл: `app/screens/SupportScreen.js`

## Что это
Чат/раздел технической поддержки.

## Простыми словами
Этот файл — одна часть фронтенда, которая либо:
- показывает UI,
- хранит состояние,
- общается с backend,
- или настраивает платформу.

## Что делает в проекте
- Импортов в файле: **12**.
- Основная ответственность: Чат/раздел технической поддержки.

## Главные элементы кода
- `SupportScreen`: отдельный шаг логики внутри этого файла.
- `buildThreadItems`: отдельный шаг логики внутри этого файла.
- `dayKey`: отдельный шаг логики внутри этого файла.
- `formatDayLabel`: отдельный шаг логики внутри этого файла.
- `formatTime`: отдельный шаг логики внутри этого файла.
- `getAttachmentName`: отдельный шаг логики внутри этого файла.
- `hasMessageRowsChanges`: отдельный шаг логики внутри этого файла.
- `initialsFromName`: отдельный шаг логики внутри этого файла.
- `isImageAttachment`: отдельный шаг логики внутри этого файла.
- `loadContacts`: отдельный шаг логики внутри этого файла.
- `loadMessages`: отдельный шаг логики внутри этого файла.
- `mergeMessages`: отдельный шаг логики внутри этого файла.
- `normalizeMessages`: отдельный шаг логики внутри этого файла.
- `onHide`: отдельный шаг логики внутри этого файла.
- `onSend`: отдельный шаг логики внутри этого файла.
- `onShow`: отдельный шаг логики внутри этого файла.
- `openAttachment`: отдельный шаг логики внутри этого файла.
- `ownAvatar`: отдельный шаг логики внутри этого файла.
- `pickAttachment`: отдельный шаг логики внутри этого файла.
- `renderAvatar`: отдельный шаг логики внутри этого файла.

## Как читать новичку
1. Сначала смотри импорты вверху.
2. Затем найди главный экспорт (`export default ...`).
3. После этого пройди функции сверху вниз — они обычно описывают полный сценарий экрана/компонента.
