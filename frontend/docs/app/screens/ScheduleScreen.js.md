# Файл: `app/screens/ScheduleScreen.js`

## Что это
Экран расписания с фильтрацией по дням и группам.

## Простыми словами
Этот файл — одна часть фронтенда, которая либо:
- показывает UI,
- хранит состояние,
- общается с backend,
- или настраивает платформу.

## Что делает в проекте
- Импортов в файле: **13**.
- Основная ответственность: Экран расписания с фильтрацией по дням и группам.

## Главные элементы кода
- `ScheduleScreen`: отдельный шаг логики внутри этого файла.
- `availableCreateTeachers`: отдельный шаг логики внутри этого файла.
- `availableEditTeachers`: отдельный шаг логики внутри этого файла.
- `editGroup`: отдельный шаг логики внутри этого файла.
- `getTodayScheduleDay`: отдельный шаг логики внутри этого файла.
- `group`: отдельный шаг логики внутри этого файла.
- `hasSaturday`: отдельный шаг логики внутри этого файла.
- `loadGroups`: отдельный шаг логики внутри этого файла.
- `match`: отдельный шаг логики внутри этого файла.
- `normalizeDayValue`: отдельный шаг логики внутри этого файла.
- `onCreateSchedule`: отдельный шаг логики внутри этого файла.
- `onDeleteEntry`: отдельный шаг логики внутри этого файла.
- `onEditEntry`: отдельный шаг логики внутри этого файла.
- `onExport`: отдельный шаг логики внутри этого файла.
- `onLoadSchedule`: отдельный шаг логики внутри этого файла.
- `onUpdateSchedule`: отдельный шаг логики внутри этого файла.
- `resetCreateForm`: отдельный шаг логики внутри этого файла.
- `resetEditor`: отдельный шаг логики внутри этого файла.
- `rowsByDay`: отдельный шаг логики внутри этого файла.
- `visibleDays`: отдельный шаг логики внутри этого файла.

## Как читать новичку
1. Сначала смотри импорты вверху.
2. Затем найди главный экспорт (`export default ...`).
3. После этого пройди функции сверху вниз — они обычно описывают полный сценарий экрана/компонента.
