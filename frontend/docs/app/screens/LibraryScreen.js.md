# Файл: `app/screens/LibraryScreen.js`

## Что это
Экран библиотеки: каталог, поиск, закладки.

## Простыми словами
Этот файл — одна часть фронтенда, которая либо:
- показывает UI,
- хранит состояние,
- общается с backend,
- или настраивает платформу.

## Что делает в проекте
- Импортов в файле: **11**.
- Основная ответственность: Экран библиотеки: каталог, поиск, закладки.

## Главные элементы кода
- `LibraryScreen`: отдельный шаг логики внутри этого файла.
- `allCatalogBooks`: отдельный шаг логики внутри этого файла.
- `filterLocalByQuery`: отдельный шаг логики внутри этого файла.
- `filteredSectionItems`: отдельный шаг логики внутри этого файла.
- `loadLocalBooks`: отдельный шаг логики внутри этого файла.
- `loadShelf`: отдельный шаг логики внутри этого файла.
- `normalizeBook`: отдельный шаг логики внутри этого файла.
- `normalizeBookKey`: отдельный шаг логики внутри этого файла.
- `normalizeShelfToBook`: отдельный шаг логики внутри этого файла.
- `normalizedItems`: отдельный шаг логики внутри этого файла.
- `onChangeGenre`: отдельный шаг логики внутри этого файла.
- `onCreateBook`: отдельный шаг логики внутри этого файла.
- `onNextPage`: отдельный шаг логики внутри этого файла.
- `onOpenBook`: отдельный шаг логики внутри этого файла.
- `onPrevPage`: отдельный шаг логики внутри этого файла.
- `onSubmitSearch`: отдельный шаг логики внутри этого файла.
- `onToggleBookmark`: отдельный шаг логики внутри этого файла.
- `onToggleFavorite`: отдельный шаг логики внутри этого файла.
- `onToggleRead`: отдельный шаг логики внутри этого файла.
- `renderBookCard`: отдельный шаг логики внутри этого файла.

## Как читать новичку
1. Сначала смотри импорты вверху.
2. Затем найди главный экспорт (`export default ...`).
3. После этого пройди функции сверху вниз — они обычно описывают полный сценарий экрана/компонента.
