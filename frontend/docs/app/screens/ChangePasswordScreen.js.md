# Файл: `app/screens/ChangePasswordScreen.js`

## Что это
Экран смены пароля.

## Простыми словами
Этот файл — одна часть фронтенда, которая либо:
- показывает UI,
- хранит состояние,
- общается с backend,
- или настраивает платформу.

## Что делает в проекте
- Импортов в файле: **9**.
- Основная ответственность: Экран смены пароля.

## Главные элементы кода
- `ChangePasswordScreen`: отдельный шаг логики внутри этого файла.
- `onChangePassword`: отдельный шаг логики внутри этого файла.

## Как читать новичку
1. Сначала смотри импорты вверху.
2. Затем найди главный экспорт (`export default ...`).
3. После этого пройди функции сверху вниз — они обычно описывают полный сценарий экрана/компонента.
