# Файл: `app/screens/ModuleDetailsScreen.js`

## Что это
Подробные баллы модулей и итоговых оценок.

## Простыми словами
Этот файл — одна часть фронтенда, которая либо:
- показывает UI,
- хранит состояние,
- общается с backend,
- или настраивает платформу.

## Что делает в проекте
- Импортов в файле: **6**.
- Основная ответственность: Подробные баллы модулей и итоговых оценок.

## Главные элементы кода
- `ModuleDetailsScreen`: отдельный шаг логики внутри этого файла.
- `moduleRatingLabel`: отдельный шаг логики внутри этого файла.
- `readableDate`: отдельный шаг логики внутри этого файла.
- `tableCells`: отдельный шаг логики внутри этого файла.

## Как читать новичку
1. Сначала смотри импорты вверху.
2. Затем найди главный экспорт (`export default ...`).
3. После этого пройди функции сверху вниз — они обычно описывают полный сценарий экрана/компонента.
