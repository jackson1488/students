# Файл: `app/screens/TestsScreen.js`

## Что это
Экран тестирования: создание/активация/сдача тестов.

## Простыми словами
Этот файл — одна часть фронтенда, которая либо:
- показывает UI,
- хранит состояние,
- общается с backend,
- или настраивает платформу.

## Что делает в проекте
- Импортов в файле: **13**.
- Основная ответственность: Экран тестирования: создание/активация/сдача тестов.

## Главные элементы кода
- `TestsScreen`: отдельный шаг логики внутри этого файла.
- `addQuestion`: отдельный шаг логики внутри этого файла.
- `adminRows`: отдельный шаг логики внутри этого файла.
- `answeredCount`: отдельный шаг логики внутри этого файла.
- `bootstrap`: отдельный шаг логики внутри этого файла.
- `buildTeacherRows`: отдельный шаг логики внутри этого файла.
- `canSubmitCurrent`: отдельный шаг логики внутри этого файла.
- `currentQuestion`: отдельный шаг логики внутри этого файла.
- `filteredAdminStudents`: отдельный шаг логики внутри этого файла.
- `formatTime`: отдельный шаг логики внутри этого файла.
- `generateQuestions`: отдельный шаг логики внутри этого файла.
- `loadTests`: отдельный шаг логики внутри этого файла.
- `makeQuestionDraft`: отдельный шаг логики внутри этого файла.
- `makeQuestionDrafts`: отдельный шаг логики внутри этого файла.
- `mapServerQuestionsToDrafts`: отдельный шаг логики внутри этого файла.
- `onActivateTest`: отдельный шаг логики внутри этого файла.
- `onAllowRetake`: отдельный шаг логики внутри этого файла.
- `onCreateTest`: отдельный шаг логики внутри этого файла.
- `onSelectAnswer`: отдельный шаг логики внутри этого файла.
- `openTestSession`: отдельный шаг логики внутри этого файла.

## Как читать новичку
1. Сначала смотри импорты вверху.
2. Затем найди главный экспорт (`export default ...`).
3. После этого пройди функции сверху вниз — они обычно описывают полный сценарий экрана/компонента.
