# Файл: `app/screens/ProfileDetailsScreen.js`

## Что это
Подробный профиль текущего пользователя.

## Простыми словами
Этот файл — одна часть фронтенда, которая либо:
- показывает UI,
- хранит состояние,
- общается с backend,
- или настраивает платформу.

## Что делает в проекте
- Импортов в файле: **12**.
- Основная ответственность: Подробный профиль текущего пользователя.

## Главные элементы кода
- `DataRow`: отдельный шаг логики внутри этого файла.
- `ProfileDetailsScreen`: отдельный шаг логики внутри этого файла.
- `avatarUri`: отдельный шаг логики внутри этого файла.
- `calculateCourse`: отдельный шаг логики внутри этого файла.
- `extractStudentCode`: отдельный шаг логики внутри этого файла.
- `load`: отдельный шаг логики внутри этого файла.
- `onSave`: отдельный шаг логики внутри этого файла.
- `resolveMediaUrl`: отдельный шаг логики внутри этого файла.

## Как читать новичку
1. Сначала смотри импорты вверху.
2. Затем найди главный экспорт (`export default ...`).
3. После этого пройди функции сверху вниз — они обычно описывают полный сценарий экрана/компонента.
