# Файл: `app/screens/SettingsScreen.js`

## Что это
Экран настроек (язык, тема, параметры).

## Простыми словами
Этот файл — одна часть фронтенда, которая либо:
- показывает UI,
- хранит состояние,
- общается с backend,
- или настраивает платформу.

## Что делает в проекте
- Импортов в файле: **8**.
- Основная ответственность: Экран настроек (язык, тема, параметры).

## Главные элементы кода
- `SettingsScreen`: отдельный шаг логики внутри этого файла.
- `openChangePassword`: отдельный шаг логики внутри этого файла.
- `openSupport`: отдельный шаг логики внутри этого файла.

## Как читать новичку
1. Сначала смотри импорты вверху.
2. Затем найди главный экспорт (`export default ...`).
3. После этого пройди функции сверху вниз — они обычно описывают полный сценарий экрана/компонента.
