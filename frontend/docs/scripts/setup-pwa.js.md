# Файл: `scripts/setup-pwa.js`

## Что это
Скрипт подготовки PWA-конфигурации и web-ассетов для установки как приложения.

## Простыми словами
Этот файл — одна часть фронтенда, которая либо:
- показывает UI,
- хранит состояние,
- общается с backend,
- или настраивает платформу.

## Что делает в проекте
- Импортов в файле: **0**.
- Основная ответственность: Скрипт подготовки PWA-конфигурации и web-ассетов для установки как приложения.

## Главные элементы кода
- `copyIcons`: отдельный шаг логики внутри этого файла.
- `ensureDir`: отдельный шаг логики внутри этого файла.
- `patchHtml`: отдельный шаг логики внутри этого файла.
- `setupForDir`: отдельный шаг логики внутри этого файла.
- `writeManifestAndSw`: отдельный шаг логики внутри этого файла.

## Как читать новичку
1. Сначала смотри импорты вверху.
2. Затем найди главный экспорт (`export default ...`).
3. После этого пройди функции сверху вниз — они обычно описывают полный сценарий экрана/компонента.
